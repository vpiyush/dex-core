
[package]
name = "ipc"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = "1.25.0"

[target.'cfg(loom)'.dependencies]
loom = "0.7.2"

[lints.rust]
unexpected_cfgs  = {level = "warn", check-cfg = ['cfg(loom)']}